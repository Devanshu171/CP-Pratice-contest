#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
  // ll n;
  // cin >> n;
  cout << (ll)pow(1378, 1) << endl;
  cout << (ll)pow(1378, 2) << endl;
  cout << (ll)pow(1378, 3) << endl;
  cout << (ll)pow(1378, 4) << endl;
  cout << (ll)pow(1378, 5) << endl;
  cout << (ll)pow(1378, 6) << endl;
  cout << (ll)pow(1378, 7) << endl;

  return 0;
}